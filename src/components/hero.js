import React from "react";
import { gsap } from "gsap";
import './hero.scss';

const Hero = () => {
  React.useEffect(() => {

  }, []);

  return (
    <section className="hero">
      Hello Hero!
    </section>
  );
};

export default Hero;
