import React from "react";
import { gsap } from "gsap";
import './hero.scss';

const Hero = () => {
  React.useEffect(() => {

  }, []);

  return (
    <section className="hero">
      HERO
    </section>
  );
};

export default Hero;
